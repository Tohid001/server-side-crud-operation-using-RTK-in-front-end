import styled from "styled-components";
import { NavLink } from "react-router-dom";

export const AddUserButton = styled(NavLink)`
  all: unset;
  position: fixed;
  border-radius: 50%;
  padding: 1em;
  font-size: 1rem;
  background: rgba(237, 218, 2);
`;
